    apiversion: apps/v1
    kind: replicaset
    metadata:
       name: myrs
       lables:
         app: myapp
         type: frontend
    spec:
       template:
         metadata:
           name: myapp-pod
           labels:
             app: myapp

         spec:
           containers:
             - name: nginx-container
               image: nginx

       replicas: 3

       selector:
         matchlabels:
           app: myapp
